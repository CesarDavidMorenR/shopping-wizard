const checkboox = document.getElementById("checkgift");

checkboox.addEventListener("click", () => {
  let appaer = document.getElementById("appear");

  // appaer.classList.remove('aparecer-gift');
  appaer.classList.toggle("bl");
  // e.preventDefault();
});
